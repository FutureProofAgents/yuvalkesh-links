---
import BlogArchive from '../../../components/BlogArchive.astro';
import SiteFooter from '../../../components/SiteFooter.astro';
import { getCollection } from 'astro:content';

const site = Astro.site!.href.replace(/\/$/, '');
const posts = (await getCollection('blog', ({ data }) => !data.draft && data.lang === 'he'))
  .sort((a, b) => b.data.pubDate.valueOf() - a.data.pubDate.valueOf());

const jsonLd = {
  '@context': 'https://schema.org',
  '@type': ['Blog', 'CollectionPage'],
  name: 'FutureProof Agents',
  url: `${site}/he/blog/`,
  mainEntity: {
    '@type': 'ItemList',
    numberOfItems: posts.length,
    itemListElement: posts.map((post, i) => ({
      '@type': 'ListItem', position: i + 1,
      url: `${site}/he/blog/${post.id}/`, name: post.data.title,
    })),
  },
  inLanguage: 'he',
  description: 'מאמרים על בינה מלאכותית עסקית, סוכני AI ובנייה עם כלים חדשים. מאת יובל קשטכר.',
  author: { '@type': 'Person', name: 'Yuval Keshtcher', url: site },
};
---
<!DOCTYPE html>
<html lang="he" dir="rtl">
<head>
  <script is:inline defer src="/scripts/analytics.js"></script>
  <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
  <link rel="icon" type="image/png" sizes="96x96" href="/favicon-96.png" />
  <link rel="apple-touch-icon" sizes="180x180" href="/apple-touch-icon.png" />
  <meta charset="UTF-8" />
  <script is:inline>(function(){var h=location.hostname;if(h==='futureproofagents.com'||h==='localhost'||h==='127.0.0.1')return;var p=location.pathname;if(p==='/'||p==='/index.html')p='/he/links/';location.replace('https://futureproofagents.com'+p+location.search+location.hash);})();</script>
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>מאמרים | FutureProof Agents</title>
  <meta name="description" content="מאמרים על בינה מלאכותית עסקית, סוכני AI ובנייה עם כלים חדשים. מאת יובל קשטכר." />
  <link rel="canonical" href={`${site}/he/blog/`} />
  <link rel="alternate" hreflang="he" href={`${site}/he/blog/`} />
  <link rel="alternate" hreflang="en" href={`${site}/blog/`} />
  <link rel="alternate" hreflang="x-default" href={`${site}/blog/`} />
  <link rel="alternate" type="application/rss+xml" title="FutureProof Agents" href={`${site}/rss.xml`} />
  <meta property="og:type" content="website" />
  <meta property="og:title" content="מאמרים | FutureProof Agents" />
  <meta property="og:description" content="מאמרים על בינה מלאכותית עסקית, סוכני AI ובנייה עם כלים חדשים. מאת יובל קשטכר." />
  <meta property="og:url" content={`${site}/he/blog/`} />
  <meta property="og:image" content={`${site}/avatar.jpg`} />
  <meta name="robots" content="index, follow, max-image-preview:large" />
  <meta name="twitter:card" content="summary_large_image" />
  <meta name="theme-color" content="#000000" />
  <script type="application/ld+json" set:html={JSON.stringify(jsonLd).replace(/</g, '\\u003c')} />

  <link rel="preconnect" href="https://fonts.googleapis.com" />
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
  <link href="https://fonts.googleapis.com/css2?family=Heebo:wght@400;500;600;700;800&display=swap" rel="stylesheet" />

</head>
<body>
  <main>
    <BlogArchive posts={posts} lang="he" />
  </main>
  <SiteFooter lang="he" />
</body>
</html>
